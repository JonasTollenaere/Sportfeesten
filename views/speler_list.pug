extends layout

block content
  h1= title
  
  ul
    each speler in speler_list
      li 
        a(href=speler.url) #{speler.voornaam} #{speler.achternaam}
        |  (#{speler.leeftijd} jaar, #{speler.thuislocatie.naam})

    else
      li Er zijn geen spelers.